fname=input("Enter file name: ")
try:
    fh=open(fname)
except:
    print('File cannot be opened:',fname)
    exit()

fh=open(fname)
count=0
for line in fh:
    if not line.startwith("X-DSPAM-Confidence:") : continue
        count=count+1
Fcount=float(count)
